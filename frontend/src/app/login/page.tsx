import { AuthScreen } from "@/features/auth/components/AuthScreen";

/** Trang xác thực người dùng (/login).
 * Alias hiển thị màn hình Đăng nhập / Đăng ký trực tiếp.
 */
export default function LoginPage() {
  return <AuthScreen />;
}
