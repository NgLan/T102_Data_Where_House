import { AuthScreen } from "@/features/auth/components/AuthScreen";

/** Trang xác thực người dùng (/auth).
 * Hiển thị màn hình Đăng nhập / Đăng ký trực tiếp.
 */
export default function AuthPage() {
  return <AuthScreen />;
}
