import { LandingScreen } from "@/features/landing";

/** Trang giới thiệu ứng dụng (/landing).
 * Hiển thị Landing Page công khai bao gồm nút Đăng nhập, Video Demo và đường dẫn Tài liệu Hướng dẫn (Placeholder).
 */
export default function LandingPage() {
  return <LandingScreen />;
}
